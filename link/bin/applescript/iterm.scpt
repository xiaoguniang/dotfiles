tell application "iTerm2"
	activate
end tell

-- on run argv
	-- tell application "iTerm2"
		-- set cmd to "zsh -l -c \"nvim " & argv & quote
		-- set newWindow to (create window with default profile command cmd)
    -- end tell
-- end run
