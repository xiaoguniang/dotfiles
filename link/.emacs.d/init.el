; (tool-bar-mode -1)


; (setq spacemacs-start-directory "~/.emacs.d/spacemacs/")
; (load-file (concat spacemacs-start-directory "init.el"))

; (require 'package)
; (setq package-archives '(("gnu"   . "http://mirrors.tuna.tsinghua.edu.cn/elpa/gnu/")
                         ; ("melpa" . "http://mirrors.tuna.tsinghua.edu.cn/elpa/melpa/")))

; (package-initialize)

; (set-frame-parameter (selected-frame) 'alpha '(85 85))

; (add-to-list 'default-frame-alist '(alpha 85 85))

; (set-face-attribute 'default nil :background "white"
  ; :foreground "black" :font "Courier" :height 150)

; (dolist (package package-list)
  ; (unless (package-installed-p package)
    ; (package-install package)))

; (add-hook 'text-mode-hook (lambda ()
				; (linum-mode 1)
				; (setq require-final-newline nil)))



; (custom-set-variables
 ; ;; custom-set-variables was added by Custom.
 ; ;; If you edit it by hand, you could mess it up, so be careful.
 ; ;; Your init file should contain only one such instance.
 ; ;; If there is more than one, they won't work right.
 ; '(package-selected-packages
   ; (quote
    ; (linum-relative relative-line-numbers zenburn-theme markdown-preview-eww magit markdown-preview-mode markdown-mode))))
; (custom-set-faces
 ; ;; custom-set-faces was added by Custom.
 ; ;; If you edit it by hand, you could mess it up, so be careful.
 ; ;; Your init file should contain only one such instance.
 ; ;; If there is more than one, they won't work right.
 ; )
